{
  "name": "resize_image_dev",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "node build.js",
    "docker:test": "docker build --platform linux/amd64 -f test/Dockerfile -t image-resize-test . && docker run --rm image-resize-test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-cloudfront": "^3.606.0",
    "@aws-sdk/client-lambda": "^3.606.0",
    "@aws-sdk/client-s3": "^3.606.0",
    "sharp": "^0.32.6"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "fs-extra": "^11.2.0",
    "jest": "^29.7.0"
  }
}
